🚀 Hostel App Backend (Node.js + MongoDB)

This is the backend API for the Hostel App, built using Node.js, Express, and MongoDB.
It includes role-based authentication for User, Hostel Owner, and Admin, along with OTP-based email verification.

📌 Features

User Registration with Email OTP

Login with JWT authentication

Persistent login (token does not expire until logout)

Role-based access (user / owner / admin)

Send OTP via email using Nodemailer

MongoDB with Mongoose

Secure password hashing with bcrypt

API structure with Controllers, Routes, and Models

Clean and scalable folder structure